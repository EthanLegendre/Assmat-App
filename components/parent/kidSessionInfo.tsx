import { View } from "react-native";
import { Child } from "@/types/enfant";
import { checkKidInSession } from "@/lib/kid/checkKidInSession";

export function KidSessionInfo(child: Child) {
    
    
    return (
        <View>
            
        </View>
    )
}